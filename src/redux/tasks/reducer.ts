import { IAction } from '../../types/reducer';

const init = {
  // TODO
};

export default function tasks(state = init, { type, payload }: IAction) {
  switch (type) {
    case :
      return {
        // TODO
      };

    case :
      return {
        // TODO
      };

    default:
      return state;
  }
}
